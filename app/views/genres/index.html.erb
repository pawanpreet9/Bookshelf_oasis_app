
<h1>All Genres</h1>
<%= paginate @genres %>
<table>
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>
  <tbody>
    <% @genres.each do |genre| %>
      <tr>
         <td><%= link_to genre.name, genre_path(genre) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
